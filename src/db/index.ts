export { 
    userExists, 
    getUserId,
    getUserPassHash,
    setUserPassHash,
    noUsers,
    getPublicKey,
} from "./user";

export {
    getMessagesForUser,
    saveMessage,
} from "./message";